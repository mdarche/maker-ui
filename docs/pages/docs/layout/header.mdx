<SEO
  title="Header"
  description="Use the Header component to wrap your primary navigation, custom widgets, and mobile navigation menu."
/>

<Diagram component="Header">

```jsx
import { Header } from 'maker-ui'
```

</Diagram>

The Header component renders a `<header>` tag that contains all components for desktop and mobile navigation.

## Props

|      Prop      | Description                                                                                                                                                                                                                        |
| :------------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|    absolute    | <Type name="boolean" /> Overrides `header.absolute` from Maker UI options.                                                                                                                                                         |
|   background   | <Type name="ResponsiveString" /> Overrides the Header's default `--color-bg_header` background value that you can set in Maker UI options. **Note:** This doesn't have to be a color - you can use a gradient or an image as well. |
|      css       | <Type name="object" /> Applies responsive CSS styles to the component.                                                                                                                                                             |
|     sticky     | <Type name="boolean"/> Overrides `header.sticky` from Maker UI options.                                                                                                                                                            |
| stickyOnMobile | <Type name="boolean"/> Overrides `header.stickyOnMobile` from Maker UI options.                                                                                                                                                    |
| stickyUpScroll | <Type name="boolean"/> Overrides `header.stickyUpScroll` from Maker UI options.                                                                                                                                                    |

<Callout type="suggestion">
  The override props are designed for conditional use cases or runtime logic.
  Whenever possible, use Maker UI options instead.
</Callout>

## Maker UI Options

Your options configuration has a `header` object with the following properties:

|         Option          | Description                                                                                                                                                                                                                                                                                                            |
| :---------------------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|        absolute         | <Type name="boolean" /> If true, the header will be absolutely positioned so site content begins at the top of the viewport. <DefaultValue value="false" />                                                                                                                                                            |
|       breakpoint        | <Type name="string-number" /> A breakpoint that controls when the header switches from desktop to mobile navigation. You may also use an index to access a breakpoint in the `options.breakpoints` array.                                                                                                              |
|       colorButton       | <Type multi={["'default'", 'React.ReactNode', '((currentMode?: string, attributes?: object) => React.ReactNode)' ]} /> Lets you customize the header's color toggle. By default, it prints the name of the current color mode, but you can use a custom React component or a JSX callback to animate the icon's state. |
|        dropdown         | <Type nested={{ caret: ['boolean', 'React.ReactElement'], transition: ["'scale'", "'fade'", "'fade-down'", "'fade-up'", "'none'"] }}></Type>                                                                                                                                                                           |
|      errorBoundary      |                                                                                                                                                                                                                                                                                                                        |
| showColorButtonOnMobile |                                                                                                                                                                                                                                                                                                                        |
|   showWidgetsOnMobile   |                                                                                                                                                                                                                                                                                                                        |
|        maxWidth         |                                                                                                                                                                                                                                                                                                                        |
|       menuButton        | <Type multi={["'default'", 'React.ReactNode', '((isOpen?: boolean, attributes?: object) => React.ReactNode)' ]} />                                                                                                                                                                                                     |
|      menuOverflow       |                                                                                                                                                                                                                                                                                                                        |
|      mobileNavType      |                                                                                                                                                                                                                                                                                                                        |
|         navType         | <Type multi={["'basic'", "'basic-left'", "'basic-right'", "'center'", "'split'", "'minimal'", "'minimal-left'","'minimal-center'","'reverse'", ]} flex />                                                                                                                                                              |
|       scrollClass       |                                                                                                                                                                                                                                                                                                                        |
|     showColorButton     |                                                                                                                                                                                                                                                                                                                        |
|         sticky          | A boolean that determines if the header sticks to the top of the viewport on mobile.                                                                                                                                                                                                                                   |
|     stickyOnMobile      |                                                                                                                                                                                                                                                                                                                        |
|     stickyUpScroll      |                                                                                                                                                                                                                                                                                                                        |

## Usage

Add `Header` to the beginning of your layout tree and wrap it around `Navbar` and `MobileMenu`:

```jsx {15-18}
import {
  Layout,
  Header,
  Navbar,
  MobileMenu
  Content,
  Main,
  Footer
} from 'maker-ui'

// This example omits key props like menus, options, etc.

export const MyLayout = ({ children }) => (
  <Layout>
    <Header>
      <Navbar />
      <MobileMenu />
    </Header>
    <Content>
      <Main>{children}</Main>
    </Content>
    <Footer />
  </Layout>
)
```
