<SEO
  title="Layout"
  description="Use the Layout component as a provider at the root of your application to take advantage of Maker UI's layout features."
/>

<Diagram component="Layout" subtitle="Provider">

```jsx
import { Layout } from 'maker-ui'
```

</Diagram>

The Layout component is Maker UI's context provider for storing all of your layout configurations. It is a required wrapper if you want to use any of the other Layout components.

## Props

## Usage
