import PageLink from "../../components/page-link"

# Layout

The Layout component is Elements' root component. It wraps your entire layout to give your components access theme options, usage hooks, and the ThemeUI Provider.

## Props

- **children** (required)
- **color** - overrides the default `text` value in your ThemeUI color object

<PageLink path="/components/topbar" label="Topbar" />
