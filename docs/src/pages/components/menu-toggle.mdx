import { Styled } from "theme-ui"
import PageLink from "../../components/page-link"
import Title from "../../components/title"

<Title type="menu-toggle" title="MenuToggle">
  <Styled.p>
    The MenuToggle is a button that opens and closes the MobileNav component. By
    default, it is hidden on desktop.
  </Styled.p>
  <Styled.p>
    You can wrap it around your own icon, text, or component, or you can use one
    of Element's default icons.
  </Styled.p>
</Title>

| Props        |            Type             |                                                                                                                       Description |
| ------------ | :-------------------------: | --------------------------------------------------------------------------------------------------------------------------------: |
| **children** |           _node_            |                                                                                                                          optional |
| **fill**     |          _string_           |                                                             overrides the default fill color specified as `navlink` in `theme.js` |
| **height**   | _string_, _number_, _array_ | sets the height of the SVG icon if you use a default icon or supply your own. Can take an array of values for responsive scaling. |
| **icon**     |          _object_           |                                                        Elements currently supports `"menu"` or `"close"`. More icons coming soon. |

## Styling

You can add extra styles with the `sx`, `css`, or `style` prop (ThemeUI, Emotion, or React).

<PageLink path="/components/mobile-nav" label="MobileNav" />
