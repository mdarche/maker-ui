// Context / reducer for all table data

// pagination, pageCount, pageSize, currentPage, data, columns (label, accessor, order, visible), style (rowStripe, rowHover, border), selectableRows, search, searchIndex,
