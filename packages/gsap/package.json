{
  "name": "@maker-ui/gsap",
  "description": "Prebuilt GSAP effects for React",
  "version": "1.2.6",
  "license": "Apache-2.0",
  "author": "Mike Darche <mike@newcreative.io>",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "docgen": "npx typedoc --tsconfig tsconfig.docs.json",
    "build": "tsup src/index.ts --format esm,cjs --dts --external react",
    "start": "tsup src/index.ts --format esm,cjs --watch --dts --external react",
    "lint": "TIMING=1 eslint src --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@maker-ui/css": "workspace:*",
    "@maker-ui/layout": "workspace:*",
    "@maker-ui/primitives": "workspace:*",
    "@maker-ui/utils": "workspace:*",
    "@use-gesture/react": "^10.2.10",
    "lodash.debounce": "^4.0.8"
  },
  "peerDependencies": {
    "gsap": "^3.9.1"
  },
  "peerDependenciesMeta": {
    "maker-ui": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/lodash.debounce": "^4.0.6",
    "eslint": "7.32.0",
    "eslint-preset": "workspace:*",
    "gsap": "^3.9.1",
    "tsconfig": "workspace:*",
    "tsup": "^5.12.0",
    "typescript": "^4.8.4"
  }
}
